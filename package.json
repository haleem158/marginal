{
  "name": "marginal",
  "version": "1.0.0",
  "description": "Decentralized Compute Allocation Market — Proof of Efficient Compute",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network og-testnet",
    "deploy:mainnet": "hardhat run scripts/deploy.ts --network og-mainnet",
    "seed": "hardhat run scripts/seed.ts --network og-testnet",
    "verify": "hardhat run scripts/verify.ts --network og-testnet",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "@nomicfoundation/hardhat-verify": "^2.0.0",
    "@types/node": "^20.0.0",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.4.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.0",
    "ethers": "^6.11.0"
  }
}
